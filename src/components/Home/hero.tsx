import React from 'react';
import AuthBackgroundShape from "~/assets/svg/auth-background-shape";

const Hero = () => {
    return (
        <div>
            <AuthBackgroundShape/>
            <div>
                WELCOME to E SHOP
            </div>
        </div>
    );
};

export default Hero;